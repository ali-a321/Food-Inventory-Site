<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1 class="headerTitle"><%= title %></h1>
   <div class="mainContainer">
    <%-include("./layout.ejs")%>

    <div class="contentContainer">
    <% if (error){ %>
       <p>"Error getting dynamic content" </p>
      <%} else { %>
     
     
      <ul > Our inventory has the following counts: 
        <li> <strong> Foods:</strong> <%= data.food_count %>  </li>
        <li> <strong>Food-types: </strong> <%= data.foodtype_count %></li>
      </ul>
      <% } %>
    </div>
    </div>

  </body>
</html>
